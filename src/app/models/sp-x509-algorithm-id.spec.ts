import { SpX509AlgorithmId } from './sp-x509-algorithm-id';

describe('SpX509AlgorithmId', () => {
  it('should create an instance', () => {
    expect(new SpX509AlgorithmId()).toBeTruthy();
  });
});
