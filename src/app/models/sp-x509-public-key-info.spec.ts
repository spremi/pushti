import { SpX509PublicKeyInfo } from './sp-x509-public-key-info';

describe('SpX509PublicKeyInfo', () => {
  it('should create an instance', () => {
    expect(new SpX509PublicKeyInfo()).toBeTruthy();
  });
});
