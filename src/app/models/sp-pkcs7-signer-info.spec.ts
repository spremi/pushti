import { SpPkcs7SignerInfo } from './sp-pkcs7-signer-info';

describe('SpPkcs7SignerInfo', () => {
  it('should create an instance', () => {
    expect(new SpPkcs7SignerInfo()).toBeTruthy();
  });
});
