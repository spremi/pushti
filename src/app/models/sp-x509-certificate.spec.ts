import { SpX509Certificate } from './sp-x509-certificate';

describe('SpX509Certificate', () => {
  it('should create an instance', () => {
    expect(new SpX509Certificate()).toBeTruthy();
  });
});
