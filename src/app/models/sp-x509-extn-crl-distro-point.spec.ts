import { SpX509ExtnCrlDistroPoint } from './sp-x509-extn-crl-distro-point';

describe('SpX509ExtnCrlDistroPoint', () => {
  it('should create an instance', () => {
    expect(new SpX509ExtnCrlDistroPoint()).toBeTruthy();
  });
});
