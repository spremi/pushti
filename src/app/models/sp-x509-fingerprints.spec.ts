import { SpX509Fingerprints } from './sp-x509-fingerprints';

describe('SpX509Fingerprints', () => {
  it('should create an instance', () => {
    expect(new SpX509Fingerprints()).toBeTruthy();
  });
});
