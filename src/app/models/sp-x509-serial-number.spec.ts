import { SpX509SerialNumber } from './sp-x509-serial-number';

describe('SpX509SerialNumber', () => {
  it('should create an instance', () => {
    expect(new SpX509SerialNumber()).toBeTruthy();
  });
});
